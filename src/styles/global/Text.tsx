import { Text, Title } from '@mantine/core';

export const TextStyle = Text.extend({
    defaultProps: { c: 'gray.8' }
});

export const TitleStyle = Title.extend({
    defaultProps: { c: 'gray.9' }
});